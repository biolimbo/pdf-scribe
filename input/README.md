# Input Folder

Place PDF files here for batch processing.

## Usage

1. Drop your PDF files into this folder
2. Run `python main.py` without arguments
3. All PDFs will be processed automatically

## Notes

- Only `.pdf` files are processed
- Files are processed in alphabetical order
- You can still specify a single file: `python main.py path/to/file.pdf`
